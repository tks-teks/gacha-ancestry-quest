Les deux fichiers 3D existent déjà dans le projet :

- `public/models/case-obus-mousgoum.glb` (~14 MB)
- `public/models/sculpture-recyclee.glb` (~14 MB)

## Action

Copier ces deux fichiers vers `/mnt/documents/` pour vous les rendre téléchargeables directement depuis le chat, puis vous fournir les liens `<presentation-artifact>` pour chacun.

Aucune modification du code ou du projet n'est nécessaire.